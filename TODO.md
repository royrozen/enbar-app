# TODO

## Now
- (none — Phase 4 below is the latest completed work; next is Phase 5)

## Next
- Review + commit + PR branch `fix/settings-desktop-layout` (currently uncommitted working-tree changes) — desktop split-console redesign of תחזוקת מכונות, part-detail expand, QR print flow, machine search, select-arrow CSS fix. See HANDOFF.md for full detail. Waiting on Roy's go/no-go before committing.
- Maintenance module Phases 5-8 (auth/role provisioning, field checklist flow, fault report flow, manager reports) — see the implementation plan for each phase's own spec
- `fault-reports` Storage bucket has no `storage.objects` RLS policies (same gap `machine-parts` had, fixed in Phase 4) — needs the same fix (public read + factory_manager/platform_admin insert) whenever the fault-report flow (Phase 7) is built, since uploads to it will otherwise fail silently
- Live browser click-through of Phases 1/2/2-revision on the Vercel preview — not yet done (phone-OTP login isn't testable in an agent session; only build + role-simulated DB checks were run)
- Auth Phase 1: complete Twilio Alphanumeric Sender ID approval, wire Supabase Auth phone OTP end-to-end
- Auth Phase 2 (RLS hardening): create `authenticated` policies → drop permissive `anon` policies → verify zero `anon` policies remain
- Auth Phase 3: convert public Storage buckets to private with signed URLs
- Backdated report logging (per-project, per-type date picker indicator) — discussed, not implemented
- Production Vercel deployment (pending the auth phases + schema cleanup)
- Decide on dropping `exception_logs.signwell_document_id` (deferred, revisit before production Vercel deployment)

## Blocked
- Twilio Alphanumeric Sender ID ("EnbarApp") — submitted for Israel, under review

## Someday
- תחזוקת מכונות add-period form (`AddPeriodForm` in `src/pages/ManagerSettings.jsx`): collapse the separate select + "+ הוספה" submit button into one control — pick a period from the "הוספת מחזור טיפול" dropdown and have it add immediately (or open the anchor inputs inline), no extra button click. Cosmetic, deferred by Roy.

## Done
- CLAUDE.md's "No real authentication (Phase 1 by design)" section rewritten (2026-09-14): replaced the stale profile-picker/shared-password description with the real phone-OTP flow (`src/pages/Login.jsx`, `src/lib/AuthContext.jsx`, `RequireProfile`/`RequireManager`/`RequireFactoryManager` in `App.jsx`). Noted `src/lib/profile.js` and `src/pages/ProfilePicker.jsx` are gone/unused. Other CLAUDE.md sections (data model, routing) may still drift from code — not audited this pass.
- Maintenance module Phase 4 (2026-09-10): parts catalog UI added as a second tab (`חלקים`, alongside `מחזורי טיפול`) inside each machine's expanded detail in `MachineCard`. List/add/edit/deactivate covering all PRD §6.5 fields (store name/phone/SKU, purchase price/date, shelf location, descriptive-only quantity, photo). `part_no` confirmed auto-assigned by the existing `trg_set_part_no` trigger (already ran correctly on Phase 3's 40 name-only imported rows — spot-checked, e.g. `003_1`.. `003_5`). Photo upload reuses the report/exception-photo pattern (`browser-image-compression`, same 1600px/1.2MB/0.85-quality options), stored in the existing `machine-parts` bucket via `machinePartPhotoUrl()` in `src/lib/supabase.js`. **Found and fixed a real Phase 1 gap**: `machine-parts` had zero `storage.objects` RLS policies (public=true only covers GET, not INSERT) — uploads would have failed silently. Added public-read + factory_manager/platform_admin-insert/update policies, matching the `machine_parts` table's own RLS shape. `fault-reports` bucket has the identical gap, left for Phase 7 (noted in Next). Build verified; UI layout screenshot-verified via a Playwright/WebKit repro against the real compiled CSS (learned from the select-arrow miss below) rather than assumed.
- Select-arrow bug (Phase 3 add-period control): `select.input` in `src/index.css` was missing `-webkit-appearance: none`, so Safari never removed native select chrome and the custom dropdown-arrow background-image didn't render. Fixed; Roy confirmed in his real browser.
- Maintenance module Phase 3 (2026-09-10): תחזוקת מכונות admin tab built (machine list #{machine_no} zero-padded/add/edit/deactivate, per-machine period assignment from the fixed 4-row catalog with schedule-appropriate anchor inputs, inline machine-owned tasks with cross-machine autocomplete, QR display+download encoding machines.id). Added `qrcode` npm dependency (approved). Imported Roy's real 21-machine CSV (`מיפוי מכונות Enbar - מכונות.csv`): 21 machines/40 parts/28 machine_periods/78 tasks, `machine_no` verified 1-21 matching CSV order (a stale identity sequence from earlier dev testing offset the first import to 6-26 — caught, fixed via `TRUNCATE machines RESTART IDENTITY CASCADE` + clean re-import). Two pre-existing bugs found and fixed along the way (approved): (1) `maintenance_periods` catalog on the dev DB never actually got the Phase 2-revision-2 fix despite TODO marking it done — dev had 4 stale rows (אחת לשלוש שנים instead of שנתי), now corrected; (2) `compute_next_due_date`'s `triannual` branch read `day_of_month` (copy-pasted from monthly) instead of `anchor_month`/`anchor_day`, contradicting the PRD's month+day anchor design — fixed to roll forward from anchor_month/anchor_day in 4-month steps. Live browser click-through was blocked in-session by real phone-OTP auth; Roy later logged in himself, confirmed the UI, and set all 28 placeholder anchors to their real values by hand.
- Maintenance module Phase 2-revision-3 (2026-09-10): dropped `maintenance_tasks` (tagged `pre-task-model-revision`, table was empty); `machine_period_tasks.task_id` FK → `task_name` text; `machine_period_log_tasks.task_id` now FKs `machine_period_tasks(id)`. Deleted `MaintenanceTasksTab`/`משימות תחזוקה`. `MachineMaintenanceTab` collapsed to a single flat tab, no sub-tab bar. Build verified.
- Maintenance module Phase 2-revision-2 (2026-09-10): deleted `אחת לשלוש שנים` seed row outright, restored plain `שנתי` (interval_years=1). Final fixed catalog: שבועי / חודשי / שנתי / תלת שנתי.
- Maintenance module Phase 2-revision (2026-09-10): מחזורי טיפול admin tab deleted entirely (periods are now fixed/seeded, never admin-editable), `triannual` schedule_kind added + `maintenance_periods` seeded with its 4 fixed rows, `compute_next_due_date` gained a triannual branch, `/manager/settings` restructured into a `תחזוקת מכונות` parent tab with `מכונות` (stub, Phase 3) and `משימות תחזוקה` (relocated) sub-tabs. Approved.
- App-wide `platform_admin` superadmin migration (2026-09-10): 40 pre-existing `factory_manager`-only RLS policies across 16 tables widened to also accept `platform_admin`, plus the `/manager/settings` route guard. Standalone change, tagged `pre-platform-admin-superadmin`.
- Maintenance module Phase 2 (2026-09-10): admin catalog tabs (מחזורי טיפול, משימות תחזוקה — the former later removed by Phase 2-revision above) and the per-employee `maintenance_access_enabled` toggle on the employees tab, in `src/pages/ManagerSettings.jsx`. Approved.
- Maintenance module Phase 1 (2026-09-10): full schema (11 new tables), RLS (all `authenticated`-only, verified via role-simulated tests), `profiles`/`employees` extended (`factory_worker`/`platform_admin` roles, self-provisioning RLS policy via `employee_access_match()`), `compute_next_due_date()` helper, `set_part_no()` trigger, `machine-parts`/`fault-reports` Storage buckets. Tagged `pre-maintenance-schema`. Approved after two rounds of verification (found and fixed: policies missing `TO authenticated`, a missing column grant, an RLS self-recursion bug in the self-provisioning policy).
- Display numbering live in schema: `clients.client_no`, `projects.project_seq`/`project_code`, `reports.report_no`, `exception_logs.exception_no` — all `IDENTITY`/unique, verified 2026-09-09 (exceptions numbering was originally deferred in the PRD but has since been implemented too)
- v1.0.0 tagged and released (git tag `v1.0.0` @ `acee99f`, GitHub Release published)
- Self-hosted signature flow live, SignWell removed from active flow
- Parts order print sheet: page-splitting bug fixed, signature area added ("שם העובד" + date)
- Lunch-ordering feature: fully designed and database-migrated
- Lunch admin tab at `/manager/settings` ("עובדים")
